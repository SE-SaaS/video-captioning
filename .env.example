# Copy to .env for local development. Never commit .env; never bake it into the image.
# Set the key(s) for whichever providers you use in config.yaml (Provider -> env var):
#   fireworks -> FIREWORKS_API_KEY, google -> GEMINI_API_KEY
FIREWORKS_API_KEY=
GEMINI_API_KEY=